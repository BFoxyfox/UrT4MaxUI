#include "ui/menudef.h"
#include "ui/theme.inc"
{

	//CONNECT MENU

	menuDef {
		#include "ui/theme_common.menu"
		#include "ui/theme_circle.menu"
		name "connect"
		background URT4_THEME_BG
		rect 0 0 640 480
		fullScreen 1
		visible 0
		style WINDOW_STYLE_SHADER
		onOpen { }
	}
}
